# Workshop Guide

- Start with a simple flawed proof (e.g., bias_variance.json).
- Ask participants to predict errors, then run analysis live.
- Discuss the JSON output structure and color-coded UI.
- Try a correct proof and compare results.
- Optionally enable `GEMINI_MOCK=1` for deterministic demo without network.

